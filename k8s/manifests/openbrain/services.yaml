apiVersion: v1
kind: Service
metadata:
  name: openbrain-mcp
  namespace: openbrain
spec:
  selector:
    app: openbrain-mcp
  ports:
    - name: http
      protocol: TCP
      port: 8000
      targetPort: 8000
